#ifndef INIT_PNML
#define INIT_PNML

/********************
 * Initialisation
 ********************/

switch(FEAT_INDUSTRIES, SELF, initPermanentStorage,
    industry_sector,
    production_rate,
    output_rate_max,
[
    SET_PERM(SECTOR, industry_sector),
    SET_PERM(PRODUCTION_RATE, production_rate),
    setPowerDemand(0, output_rate_max),

    // must not return 0
    PRODUCTION_BASE
]) { return; }

#endif // INIT_PNML
