#ifndef INDUSTRY_REGISTERS_PNML
#define INDUSTRY_REGISTERS_PNML

// Permanent registers
#define REG_PRODUCTION_RATE    0x00
#define REG_ABANDONMENT_PERIOD 0x01

#define REG_DEBUG_0 0xFC
#define REG_DEBUG_1 0xFD
#define REG_DEBUG_2 0xFE
#define REG_DEBUG_3 0xFF

// Register access
#define REG_NAME(name)        REG_##name
#define GET_PERM(name)        LOAD_PERM(REG_NAME(name))
#define SET_PERM(name, value) STORE_PERM(value, REG_NAME(name))

#endif // INDUSTRY_REGISTERS_PNML
